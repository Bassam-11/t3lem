right = ("false") 
i = 0 
car_info = input ("your car company is :") 
car_type = input ("your car type is :") 
if car_info == ("honday") :
	print ("your car type is :")  
		if car_type == ("sonata"): 
	print ("good car")
	print ("the spare parts not so expensive")
		if car_type == ("acoord") or ("elintra") or ("acsint") :
	print ("good car")
	print ("the spare parts not so expensive") 
if car_info = ("doge"): 
	print ("your car type is :")
if car_type == ("charger") or ("challenger") or ("ram")	: 
	print	("cool car") 
	print ("but the spare parts so expensive") 
if car_info == ("lexus"):
	print ("your car type is : ")
	